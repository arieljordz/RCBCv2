@{
    ViewData["Title"] = "Login";
}
<div class="row d-flex justify-content-center" style="padding-top: 200px;">
    <div class="card">
        <div class="card-body bg-light-blue p-5">
            <div class="d-flex justify-content-center pl-3 pr-3 pt-3">
                <h2 class="h2-bold">RCBC DPU TELLERLESS PORTAL</h2>
            </div>
            <section class="p-5 bg-white">
                @using (Html.BeginForm("Logout", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    <div class="d-flex justify-content-center">
                        <label>Your account is already been logged in other workstation.</label>
                    </div>
                    <div class="d-flex justify-content-center">
                        <label>Please logout first and try again.</label>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-md col-md-12" id="btnOK">OK</button>
                    </div>
                }
            </section>

        </div>
    </div>
</div>