@{
    ViewData["Title"] = "Login";
}
<div class="row d-flex justify-content-center" style="padding-top: 200px;">
    <div class="card">
        <div class="card-body bg-light-blue p-5">
            <div class="d-flex justify-content-center pl-3 pr-3 pt-3">
                <h2>RCBC DPU TELLERLESS PORTAL</h2>
            </div>
            <section class="p-5 bg-white">
                @using (Html.BeginForm("Signout", "Home", FormMethod.Post, new { enctype = "multipart/form-data", id = "formLogin" }))
                {
                    <div class="d-flex justify-content-center">
                        <label>We have signed out you due ti inactivity.</label>
                    </div>
                    <div class="d-flex justify-content-center">
                        <label>Please Login again.</label>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-md col-md-12" id="btnProceed">OK</button>
                    </div>
                }
       
            </section>

        </div>
    </div>
</div>