@model List<AuditLogsModel>

<!-- Your HTML structure for displaying the data goes here -->
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Module</th>
            <!-- Add more columns as needed -->
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Module</td>
                <!-- Add more columns as needed -->
            </tr>
        }
    </tbody>
</table>
